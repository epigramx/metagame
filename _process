Venv and package installation
    1. python3 -m venv myenv (once)
    2. source myenv/bin/activate
    3. pip install -r requirements.txt

to latest versions (when in the venv): 
    pip install --upgrade -r requirements.txt
