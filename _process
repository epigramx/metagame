Venv and package installation and updating
    1. python3 -m venv myenv
        (only done once)
    2. source myenv/bin/activate
        (every time)
    3. pip install -r requirements.txt
        (python packages)

to latest versions (when in the venv):
    pip install --upgrade -r requirements.txt

running
    A)
        source myenv/bin/activate
    B)
        go to d0nkey (Decks page)
        select up to player's "Class"(not opponent Class) (you must select a player class X 11)
            for DK: e.g. Standard | Diamond-Legend | Past Day | All Regions | Death Knight)
                PS most other filters don't affect the following data copy pasted
        copy paste the "Vs Class" to the corresponding txt file (e.g. dk.txt) (X11 times)
    C)  ./parse_text_files.sh
        ./bounds.py
            and see the PDF and other generated file
